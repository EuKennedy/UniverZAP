# https://github.com/ondrejbartas/sidekiq-cron
# use https://crontab.guru/ to validate
# validations for this file exist in /spec/configs/schedule_spec.rb

# executed daily at 0000 UTC
# schedules daily deferred jobs at stable times for each installation
internal_check_new_versions_job:
  cron: '0 0 * * *'
  class: 'Internal::TriggerDailyScheduledItemsJob'
  queue: scheduled_jobs
# # executed At every 5th minute..
trigger_scheduled_items_job:
  cron: '*/5 * * * *'
  class: 'TriggerScheduledItemsJob'
  queue: scheduled_jobs

# executed hourly for scheduled jobs that do not need minute-level cadence
trigger_hourly_scheduled_items_job:
  cron: '0 * * * *'
  class: 'Internal::TriggerHourlyScheduledItemsJob'
  queue: scheduled_jobs

# executed At every minute..
trigger_imap_email_inboxes_job:
  cron: '*/1 * * * *'
  class: 'Inboxes::FetchImapEmailInboxesJob'
  queue: scheduled_jobs

# Kanban automation engine — scans for tasks that crossed their due_date
# in the last 15min window and emits `task_overdue` events. Cadence is
# 5min so the LOOKBACK in the job covers ~3x for delivery slack.
kanban_automations_overdue_scanner_job:
  cron: '*/5 * * * *'
  class: 'Kanban::Automations::OverdueScannerJob'
  queue: default

# Tasks module — emits per-assignee `task_overdue` Notifications for
# every Task whose due_date crossed in the last 15min and is not
# done/cancelled. Cron cadence matches the LOOKBACK exactly.
tasks_overdue_scanner_job:
  cron: '*/15 * * * *'
  class: 'Tasks::OverdueScannerJob'
  queue: default

# Tasks module — hourly sweeper that spawns the next occurrence for
# any recurring template whose `next_occurrence_at` is due. The
# happy-path (user ticks the task) is handled by the `done` callback;
# this picks up tasks left open through their next firing window.
tasks_recurrence_scheduler_job:
  cron: '0 * * * *'
  class: 'Tasks::RecurrenceSchedulerJob'
  queue: default

# executed daily at 2230 UTC
# which is our lowest traffic time
remove_stale_contact_inboxes_job.rb:
  cron: '30 22 * * *'
  class: 'Internal::RemoveStaleContactInboxesJob'
  queue: scheduled_jobs

# LGPD Art. 18 VI — purga final 30 dias após scheduled_for_deletion_at.
lgpd_purge_expired_users_job:
  cron: '15 3 * * *'
  class: 'Lgpd::PurgeExpiredUsersJob'
  queue: scheduled_jobs

# Limpa dedup rows de webhooks Univercart > 90 dias.
lgpd_purge_processed_webhook_events_job:
  cron: '30 3 * * *'
  class: 'Lgpd::PurgeProcessedWebhookEventsJob'
  queue: scheduled_jobs

# executed daily at 2230 UTC
# which is our lowest traffic time
remove_stale_redis_keys_job.rb:
  cron: '30 22 * * *'
  class: 'Internal::RemoveStaleRedisKeysJob'
  queue: scheduled_jobs

# DISABLED: investigating if this job is the source of orphan conversations
# #executed daily at 0430 UTC
# # which will be IST 10:00 AM
# process_stale_contacts_job:
#   cron: '30 04 * * *'
#   class: 'Internal::ProcessStaleContactsJob'
#   queue: housekeeping

# executed daily at 0100 UTC
# to delete accounts marked for deletion
delete_accounts_job:
  cron: '0 1 * * *'
  class: 'Internal::DeleteAccountsJob'
  queue: scheduled_jobs

# executed every 30 minutes for assignment_v2
periodic_assignment_job:
  cron: '*/30 * * * *'
  class: 'AutoAssignment::PeriodicAssignmentJob'
  queue: scheduled_jobs

# executed daily at 2230 UTC
# removes old notifications (>1 month) and trims to 300 per user
remove_old_notification_job:
  cron: '30 22 * * *'
  class: 'Notification::RemoveOldNotificationJob'
  queue: purgable

# Nula o system_prompt (coluna gorda ~80k, write-only) de invocations > 30 dias,
# mantendo custo/tokens/latencia. Evita o ai_invocations inchar com o trafego.
prune_ai_invocation_logs_job:
  cron: '45 3 * * *'
  class: 'Ai::PruneInvocationLogsJob'
  queue: purgable

# executed every 12 hours
# to remove orphan conversations without contact
remove_orphan_conversations_job:
  cron: '0 */12 * * *'
  class: 'Internal::RemoveOrphanConversationsJob'
  queue: housekeeping

# Gerente: uma varredura por conta, toda segunda de madrugada. Semanal e não
# diária porque a janela analisada é de 30 dias (Ai::Manager::AnalysisService::
# WINDOW_DAYS), e uma auditoria diária repetiria a mesma conclusão seis vezes.
# Este cron precisa continuar batendo com CADENCE no mesmo serviço: é dela que a
# tela tira a data da próxima varredura.
ai_manager_weekly_scheduler_job:
  cron: '0 4 * * 1'
  class: 'Ai::Manager::WeeklySchedulerJob'
  queue: scheduled_jobs
