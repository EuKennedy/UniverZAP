{
  "_comment": "Lighthouse CI budgets. The audit workflow reads from this file when invoked with `lhci autorun`; the manual `lighthouse` command in lighthouse-ci.yml is the simpler one-shot used today and ignores most of these knobs, but keeping the file in repo signals our perf budgets clearly.",
  "ci": {
    "collect": {
      "numberOfRuns": 1,
      "url": ["https://app.univerzap.cloud/status"]
    },
    "assert": {
      "preset": "lighthouse:no-pwa",
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.7 }],
        "categories:accessibility": ["error", { "minScore": 0.85 }],
        "categories:best-practices": ["warn", { "minScore": 0.8 }],
        "categories:seo": ["warn", { "minScore": 0.85 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
